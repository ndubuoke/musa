name: Build and Verify Docker Image for Multi-Platform

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Check out the repository
      - name: Checkout Code
        uses: actions/checkout@v2

      # Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # Log in to Docker Hub
      - name: Log in to Docker Hub
        run: |
          echo "dckr_pat_P-jkYF-QC9HWMmHVL39dBmKIbg0" | docker login -u "ndubuoke" --password-stdin

      # Build and push the Docker image for multiple platforms
      - name: Build and Push Docker Image for Multiple Platforms
        run: |
          docker buildx build \
            --platform linux/amd64,linux/arm64 \
            --tag ndubuoke/auth:latest \
            --push .

      # Pull the image for verification
      - name: Pull the Image for ARM64 Verification
        run: |
          docker pull --platform linux/arm64 ndubuoke/auth:latest

      # Verify the image architecture
      - name: Verify Image Platform
        run: |
          ARCH=$(docker inspect ndubuoke/auth:latest --format '{{.Architecture}}')
          if [ "$ARCH" != "arm64" ]; then
            echo "Error: Image is not built for arm64 platform. Detected: $ARCH"
            exit 1
          fi
          echo "Image successfully built and verified for arm64 platform!"
